1.Crie uma função que receba dois números e retorne o maior.

def maior_numero(a, b):
    if a > b:
        return a
    else:
        return b

# Pedindo os números ao usuário
num1 = float(input("Digite o primeiro número: "))
num2 = float(input("Digite o segundo número: "))

# Chamando a função
resultado = maior_numero(num1, num2)

print("O maior número é:", resultado)

2.Crie uma função que receba um número e diga se ele é primo.

def eh_primo(numero: int):
    if numero <= 1:
        return 'Nao primo'
    
    for i in range(2, numero):
        if numero % i == 0:
            return 'Nao primo'
    
    return 'Primo'

numero = int(input("Digite um número: "))
print(eh_primo(numero))

3.Crie uma função que receba uma lista de números e retorne a média

def calcular_media(lista):
    if len(lista) == 0:
        return 0
    return sum(lista) / len(lista)

entrada = input("Digite números separados por espaço: ")
numeros = [float(x) for x in entrada.split()]

print("A média é:", calcular_media(numeros))

EX:
try:
    numerador = int(input('numerador: '))
    denominador = int(input('denominador: '))
    resultado = numerador / denominador
    print(f'Resultado: {resultado}')
except ZeroDivisionError:
    print('Nao e possivel fazer divisao por 0')

EX 2:
def incluir_contato(agenda, nome: str, telefone: str):
    # Verificar se contato existe
    for a in agenda:
        if a == telefone:
            print('Contato ja existe')
            return
        
        # Adicionar contato
        agenda.append(telefone)

print('Bem-vindo - agenda 2026')
lista_contatos = []
while True:
    print('Escolher uma opçao')
    print('1. Adicionar contato')
    print('2. Remover contato')
    print('3. verificar contato')
    print('4. Exibir contato')
    print('5. Sair')

    opcao = input('Opçao: ')

    if opcao == '1':
        # adicionar
        contato = input('digite o telefone: ')
        lista_contatos
        pass
    elif opcao == '2':
        # Remover
        pass
    elif opcao == '3':
        # Consultar
        pass
    elif opcao == '4':
        # Mostrar tudo
        pass
    elif opcao == '5':
        break
    else:
        print('Opçao invalida')
